package com.elegantgalaxy.tvlauncher

import android.app.Application

/**
 * Placeholder for app-wide singletons (analytics, crash reporting, a
 * PackageManager change receiver that keeps the launcher's app list in
 * sync). Kept empty on purpose — add init logic here rather than in
 * MainActivity so it also runs for any future non-UI entry points.
 */
class TvLauncherApplication : Application()
