# Copy this file to keystore.properties (git-ignored) and fill in real
# values after generating the release keystore — see README "Signing".
storeFile=../release-keystore.jks
storePassword=CHANGE_ME
keyAlias=elegantgalaxy-tv-launcher
keyPassword=CHANGE_ME
