// Top-level build file. Plugin versions are declared here (but not applied)
// so every module resolves the same version without repeating numbers.
plugins {
    id("com.android.application") version "9.3.0" apply false
}
